/* global define */

/**
 * A utility module.
 * @module common/util
 */
define(['jquery', 'ic/ic'], function($, ic) {

    'use strict';

    /*
   var $checkbox = $(".design-checkbox");

    var setFormElement = function() {
        if ($checkbox.length > 0) {
            $checkbox.each(function() {
                var self = $(this);
                self.find("input:checkbox").on({
                    change: function() {
                        if ($(this).is(":checked")) self.find(".checkbox").addClass("checked");
                        else self.find(".checkbox").removeClass("checked");
                    }
                });
            });
        }
    };
    setFormElement();
	*/
});
